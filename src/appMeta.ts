// Product name + version shown in the UI (header, landing, page title).
// The bundle APP_ID stays 'voyage-speed-planner-sl': it is stamped inside
// saved .json files, so renaming it would only churn the on-disk record.
export const APP_NAME = 'Wake';
export const APP_VERSION = '1.0';
